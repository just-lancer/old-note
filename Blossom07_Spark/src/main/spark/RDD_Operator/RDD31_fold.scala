package RDD_Operator

object RDD31_fold {
  def main(args: Array[String]): Unit = {
    /*
    RDD行动算子

    fold算子

    函数声明：
    def fold(zeroValue: T)(op: (T, T) => T): T

    函数作用：当行动算子aggregate的分区内计算规则和分区间的计算规则相同时，可以使用fold代替

     */
  }

}
