package RDD_Operator

object RDD28_take {
  def main(args: Array[String]): Unit = {
    /*
    RDD行动算子

    take算子

    函数声明：
    def take(num: Int): Array[T]

    函数作用：返回一个由RDD的前n个元素组成的数组
     */
  }

}
