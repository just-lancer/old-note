package RDD_Operator

object RDD29_takeOrdered {
  def main(args: Array[String]): Unit = {
    /*
    RDD行动算子

    takeOrdered算子

    函数声明：
    def takeOrdered(num: Int)(implicit ord: Ordering[T]): Array[T]

    函数作用：返回RDD排序后的前n个元素组成的数组
     */
  }

}
