package RDD_Operator

object RDD24_reduce {
  def main(args: Array[String]): Unit = {
    /*
    RDD行动算子

    reduce算子

    函数声明：
    def reduce(f: (T, T) => T): T

    函数作用：对RDD中的所有元素进行聚合，先聚合分区内数据，再聚合分区间数据。聚合计算为两两聚合
     */
  }

}
